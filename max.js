const x = 400;
const y = 500;
const z = 300;

// const maxx = Math.max(x, y, z);
// const maxxx = Math.min(x, y, z);

// console.log(maxx);
// console.log(maxxx);

const findLargest = (x, y, z) => {
    
    const maxx = Math.max(x, y, z);
    console.log(maxx);
};

findLargest(x, y, z)

const findmini = (x, y, z) => {
    
    const maxxx = Math.min(x, y, z);
    console.log(maxxx);
};

findmini(x,y,z)

